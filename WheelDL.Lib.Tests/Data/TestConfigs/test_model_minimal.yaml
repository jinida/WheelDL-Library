# Minimal model configuration (missing most optional fields)
task: detect
num_classes: 10

# Only essential fields, everything else should use defaults
backbone:
  - [-1, 1, Conv, [64, 3, 2]]

head:
  - [[1], 1, Detect, [num_classes]]
