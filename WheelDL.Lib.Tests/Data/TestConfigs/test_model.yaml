# Test model configuration
num_classes: 10
default_act: ReLU

scale:
  width_multiple: 0.5
  depth_multiple: 0.33
  max_channels: 512

head:
  - [-1, 1, Conv, [256, 1, 1]]
  - [-1, 1, Detect, [num_classes]]
